# Prefer `pip install -e .` (uses pyproject.toml) -- this file is kept for tools that
# expect a requirements.txt.
requests>=2.31
SQLAlchemy>=2.0
psycopg2-binary>=2.9
PyYAML>=6.0
python-dotenv>=1.0
pytest>=8.0
